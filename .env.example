# URL do frontend autorizado no CORS.
# Em producao este valor vem do SSM Parameter Store, nao daqui.
CLIENT_URL=http://localhost:5173

# Dataset do Kaggle usado por train_model.py (nao afeta a API)
DATASET_PATH=yasserh/nyc-taxi-trip-duration
